package brownshome.modding.util;

import brownshome.modding.LoadingStageName;

public enum PredefinedLoadingStages implements LoadingStageName {
	/** A stage fired to indicate that the stages process has started */
	START,

	/** A stage fired to indicate that the stages process has finished */
	END;
}
